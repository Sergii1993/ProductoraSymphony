<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Productora{% endblock %}</title>
        {% block stylesheets %}<link rel="stylesheet" type="text/css" href="{{asset('build/css/FORMATO.css')}}">{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
      
    </head>
    
    <body onload= "mueveReloj()"> 
                              {% block javascripts %}
<script language="JavaScript">
function mueveReloj(){
    momentoActual = new Date()
    hora = momentoActual.getHours()
    minuto = momentoActual.getMinutes()
    segundo = momentoActual.getSeconds()

    horaImprimible = hora + " : " + minuto + " : " + segundo

    document.form_reloj.reloj.value = horaImprimible

    setTimeout("mueveReloj()",1000)
}
</script> 
<form name="form_reloj">
<input type="text" name="reloj" size="10">
</form> 
{% endblock %}
        {% block cabecera %} 
            <div id=cabecera>

      
        

<img src="{{asset('build/img/twist-25_2.jpg')}}" WIDTH=200 HEIGHT=200 ALT="Cabeza movil"/>
<p align=center> S.RUBIO PRODUCCIONES</p>
        </div>
        <ul class=Lista>
			<li><a href="{{ path('homepage')}}">Inicio</a></li>
			<li class="dropdown">
			    <a href="{{path('productos')}}" class="dropa">Productos</a>
		       <div class="dropdown-content">
					<a href="{{path('iluminacion_index')}}">iluminacion</a>
					<a href="{{path('sonido_index')}}">sonido</a>
					
				 </div>
			</li>
			<li><a href="{{path('nosotros')}}">QuienesSomos?</a></li>
			<li><a href="{{path('contacto')}}">Contacto</a></li>
        </ul>
        {%endblock%}
        
     {% block contenido %}
       
     {% endblock %}
     
     {% block tabla %}
         {% endblock %}
     {% block tabla1 %}
         {% endblock %}

     
        
        
        
    </body>
</html>